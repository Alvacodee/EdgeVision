// sanity_check.hpp — fungsi dummy untuk validasi toolchain di Fase 0.
// Sengaja tidak punya isi "beneran", cuma buat mastiin pipeline
// C++ -> (native g++) dan C++ -> wasm -> Next.js sama-sama nyambung
// sebelum kita bangun vision-core sungguhan di Fase 1.

#pragma once

int add(int a, int b);
